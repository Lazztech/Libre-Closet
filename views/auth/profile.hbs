{{>navbar}}
<main class="flex flex-col justify-center items-center h-full gap-3">
  <h1 class="text-2xl">{{user.email}}</h1>
  <a class="link" href="{{ingressPath}}/auth/update-email">{{t 'lang.UPDATE_EMAIL'}}</a>
  <a class="link" href="{{ingressPath}}/auth/reset?email={{user.email}}"
    >{{t 'lang.CHANGE_PASSWORD'}}</a
  >
  {{#if authEnabled}}
  <a class="link" href="{{ingressPath}}/wardrobe-share/manage">{{t 'lang.WARDROBE_SHARING'}}</a>
  {{/if}}
  <a class="link" href="{{ingressPath}}/auth/delete-account">{{t 'lang.DELETE_ACCOUNT'}}</a>
</main>
{{>dock}}
